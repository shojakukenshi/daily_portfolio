%p#notice= notice
%h1 Posts
%table.table.table-striped
  %thead
    %tr
      %th Subject
      %th Content
      %th{:colspan => "3"}
  %tbody
    - @posts.each do |post|
      %tr
        %td= post.subject
        %td= post.content
        %td= link_to 'Show', [:login, post]
        %td= link_to 'Edit', edit_login_post_path(post)
        %td= link_to 'Destroy', [:login, post], method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Post', new_login_post_path
